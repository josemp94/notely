CONTINÚA un trabajo a medias en el repo Notiono (clon de Notion: Next.js 15 App Router + TS + tRPC + Prisma + Postgres + Tailwind + BlockNote). En una sesión anterior te quedaste a medias implementando la FUNCIÓN "Galería de plantillas" cuando se cortó por límite. Ya hay trabajo sin commitear: `src/lib/templates.ts` (nuevo) y cambios en `src/server/routers/pages.ts` (probablemente un endpoint createFromTemplate a medio). 

TU TAREA: revisa lo que ya hay y TERMINA la función de plantillas de forma coherente y completa. Requisitos de la función:
- Endpoint tRPC pages.createFromTemplate({ key, parentId? }) que construye server-side, de forma transaccional, una página/BD prehecha según `key` en el workspace del usuario, y devuelve su id. Al MENOS 5 plantillas en español:
  1. "tareas" → BD Tareas: campos Nombre(texto), Estado(status: Sin empezar/En curso/Hecho), Prioridad(select: Alta/Media/Baja), Fecha límite(date). Vistas Tabla + Kanban por Estado. 2-3 registros de ejemplo.
  2. "notas-reunion" → página doc con acta (encabezados: Asistentes, Orden del día, Acuerdos, Tareas).
  3. "crm" → BD Contactos: Nombre, Empresa(texto), Email(correo), Teléfono(teléfono), Estado(select: Lead/Cliente/Perdido).
  4. "presupuesto" → BD: Concepto(texto), Categoría(select), Importe(número o currency), Fecha(date). Vista Tabla (con fila de totales Suma en Importe si es sencillo).
  5. "wiki" o "diario" → página doc con estructura básica.
  Reutiliza los tipos de Field existentes y el mismo estilo con el que db.create/addField/addView/addRecord y pages.create/updateContent crean datos (o prisma directo transaccional). Mira cómo se generan `order` (índice fraccional, dep fractional-indexing) y los ids de opciones de select/status en el resto del código para que las vistas Kanban/tags funcionen igual.
- UI: un botón "✨ Plantillas" (en el sidebar, cerca de "+ Página"/"+ BD", o en su desplegable) que abre un modal-galería con tarjetas (icono + nombre + descripción corta) de las plantillas; al elegir una, llama al endpoint, invalida el árbol (utils.pages.tree.invalidate()) y navega a la página creada. Estilo de marca: naranja #ff5c28, var(--border), var(--muted).

IMPORTANTE:
- NO rompas ni deshagas la función ya terminada y commiteada "publicar página" (ruta /s/<token>, popover 🌐). 
- Si `src/lib/templates.ts` o el endpoint a medias tienen errores o están incompletos, corrígelos/complétalos.
- Ejecuta `npm run build` y déjalo LIMPIO (arregla todos los errores TS). Si tocas el schema, migración a mano + `npx prisma generate` (probablemente NO haga falta para plantillas).
- git add -A && commit con un único mensaje claro para la función de plantillas. NO hagas push. Imprime DOBBY_DONE + el hash del commit.
